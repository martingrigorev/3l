version: '3.8'

services:
  lev-learn-letters:
    container_name: lev-learn-letters
    build: .
    restart: unless-stopped
    ports:
      - "3005:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/app/data/progress.db
